<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= categoryName %></title>
</head>
<body>

    <%- include('./partials/nav') %>

      <h1><%= categoryName %></h1>
      <hr>
      <ol>
        <% menuItems.forEach(item => { %>
          <li>
            <h3><%= item.name %></h3>
            <ul>
              <li><b>Price:</b> <%= item.price.toFixed(2) %></li>
              <li><b>Rating:</b><%= item.rating %> stars</li>
              <li><%= item.details %></li>
            </ul>
          </li>
        <% }); %>
      </ol>
</body>
</html>